 这是一个瑞典电价与电力波动预测AI项目，从2月17日开始到6月5日结束。我来为你详细拆解每个阶段的执行步骤：

---

## 项目全景图

| 时间周期 | 阶段 | 核心任务 | 颜色标识 |
|---------|------|---------|---------|
| 2.17 | 阶段0：前置准备 | 仓库创建、项目结构搭建 | 🔵 前置/初始化 |
| 2.18-3.2 | 阶段1：环境&数据获取 | 环境配置、数据下载、EDA初版 | 🔵 前置/初始化 |
| 3.3-3.30 | 阶段2：核心特征工程 | 特征开发、Pipeline搭建 | 🟢 核心特征 |
| 4.1-4.27 | 阶段3：辅助特征+建模 | 辅助特征集成、模型训练 | 🟡 建模（VG核心） |
| 5.1-5.20 | 阶段4：Streamlit开发 | 应用开发、可视化 | 🔴 应用开发 |
| 5.21-5.31 | 阶段5：报告&代码重构 | 报告撰写、代码规范 | 🟣 报告/提交 |
| 6.1-6.5 | 阶段6：最终提交 | 测试、提交 | 🟣 报告/提交 |

---

## Step by Step 执行指南

### 🔵 阶段0：前置准备（2月17日，单日）

**目标**：搭建项目骨架，建立版本控制

**执行步骤**：

1. **创建GitHub私有仓库**
   ```bash
   # 在GitHub创建名为 swedish-electricity-forecast 的私有仓库
   git init
   git remote add origin https://github.com/yourusername/swedish-electricity-forecast.git
   ```

2. **搭建本地项目结构**
   ```
   swedish-electricity-forecast/
   ├── data/
   │   ├── raw/          # 原始数据
   │   ├── processed/    # 清洗后数据
   │   └── external/     # 外部辅助数据
   ├── src/
   │   ├── __init__.py
   │   ├── utils.py      # 工具函数
   │   ├── data_processor.py  # 数据处理类
   │   ├── features.py   # 特征工程
   │   └── models.py     # 模型定义
   ├── notebooks/        # Jupyter笔记本（EDA用）
   ├── app/              # Streamlit应用
   ├── tests/            # 单元测试
   ├── models/           # 保存的模型文件
   ├── reports/          # 报告和图表
   ├── requirements.txt
   ├── README.md
   └── .gitignore
   ```

3. **编写README基础版**
   - 项目简介（瑞典电价预测）
   - 数据来源说明
   - 安装指南
   - 项目结构说明

4. **制定数据采集规范**
   - 记录Nord Pool API接口文档
   - 记录Svenska Kraftnät数据下载方式
   - 确定数据存储格式（CSV/Parquet）
   - 定义数据更新频率

**里程碑⭐**：完成GitHub首次提交，commit信息：`[INIT] 项目初始化，搭建基础结构`

---

### 🔵 阶段1：环境&数据获取（2月18日-3月2日，2周）

#### 第1周（2.18-2.23）：环境+数据下载

**执行步骤**：

1. **配置Python虚拟环境**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Windows: venv\Scripts\activate
   pip install pandas numpy scikit-learn xgboost matplotlib seaborn streamlit requests
   pip freeze > requirements.txt
   ```

2. **编写`src/utils.py`工具函数**
   ```python
   # 包含功能：
   # - 日志配置
   # - 数据下载函数（Nord Pool API）
   # - 数据保存/加载函数
   # - 时间序列处理工具
   ```

3. **下载核心数据**
   - **Nord Pool数据**：历史电价（SE1-SE4区域）
   - **Svenska Kraftnät数据**：电力生产/消费数据
   - 数据时间范围：建议至少2-3年历史数据
   - 保存到`data/raw/`

4. **基础数据验证**
   - 检查数据完整性（缺失值统计）
   - 检查时间连续性
   - 基础统计描述

#### 第2周（2.24-3.2）：EDA初版+周报

**执行步骤**：

1. **核心EDA初版（3张关键图）**
   - 图1：电价时间序列趋势图（日线/小时线）
   - 图2：电价分布直方图（识别异常值）
   - 图3：区域电价对比（SE1-SE4热力图或箱线图）
   - 保存到`reports/figures/`

2. **编写`DataProcessor`类骨架**
   ```python
   class DataProcessor:
       def __init__(self, raw_data_path):
           pass
       
       def load_data(self):
           pass
       
       def clean_data(self):
           pass
       
       def get_features(self):
           pass
   ```

3. **发送第一封周报给导师**
   - 本周完成内容
   - 数据质量评估
   - 下周计划（特征工程）

**里程碑⭐**：完成EDA初版+DataProcessor骨架，commit：`[DATA] 完成数据获取与初步EDA`

---

### 🟢 阶段2：核心特征工程（3月3日-3月30日，4周）

#### 第3-4周（3.3-3.16）：深度特征开发

**执行步骤**：

1. **深度数据清洗**
   - 处理缺失值（插值/删除策略）
   - 异常值检测与处理（电价极端值）
   - 时区统一处理（UTC+1/CET）

2. **实现`add_power_features()`核心特征**
   ```python
   def add_power_features(df):
       # 时间特征
       df['hour'] = df.index.hour
       df['day_of_week'] = df.index.dayofweek
       df['month'] = df.index.month
       df['is_weekend'] = (df.day_of_week >= 5).astype(int)
       
       # 滞后特征（过去24小时、7天电价）
       for lag in [1, 2, 3, 24, 168]:  # 1h, 2h, 3h, 1d, 1w
           df[f'price_lag_{lag}'] = df['price'].shift(lag)
       
       # 滚动统计特征
       df['price_roll_mean_24h'] = df['price'].rolling(24).mean()
       df['price_roll_std_7d'] = df['price'].rolling(168).std()
       
       # 周期性编码
       df['hour_sin'] = np.sin(2 * np.pi * df['hour'] / 24)
       df['hour_cos'] = np.cos(2 * np.pi * df['hour'] / 24)
       
       return df
   ```

3. **特征相关性筛选**
   - 计算特征与目标变量的相关系数
   - 移除高共线性特征（VIF分析）
   - 保存特征重要性排序

4. **EDA特征分析**
   - 特征分布可视化
   - 特征与目标关系图（散点图、热力图）

#### 第5-6周（3.17-3.30）：Pipeline+中期周报

**执行步骤**：

1. **搭建预处理Pipeline**
   ```python
   from sklearn.pipeline import Pipeline
   from sklearn.preprocessing import StandardScaler
   
   preprocessing_pipeline = Pipeline([
       ('feature_engineering', PowerFeatureAdder()),
       ('scaler', StandardScaler()),
       ('imputer', SimpleImputer(strategy='mean'))
   ])
   ```

2. **时间序列数据集划分**
   - 训练集：前80%（按时间顺序，非随机）
   - 测试集：后20%
   - 验证策略：滚动窗口或扩展窗口

3. **完善`DataProcessor`类**
   - 实现完整的`fit_transform`流程
   - 添加数据质量检查方法
   - 添加特征存储/加载功能

4. **发送中期周报**
   - 特征工程成果展示
   - 特征重要性初步结果
   - 建模阶段计划

**里程碑⭐**：完成特征工程Pipeline，commit：`[FEATURE] 完成核心特征工程与Pipeline搭建`

---

### 🟡 阶段3：辅助特征+建模（4月1日-4月27日，4周）⭐VG核心阶段

#### 第7-8周（4.1-4.13）：辅助特征+基线模型 ⭐VG节点1

**执行步骤**：

1. **下载/处理工业/温度辅助特征**
   - **温度数据**：瑞典主要城市历史气温（SMHI气象数据）
   - **工业数据**：工业用电指数、生产指数
   - **节假日数据**：瑞典公共假期标记
   - 数据对齐：按时间戳合并到主数据集

2. **实现`add_industrial_auxiliary_features()`**
   ```python
   def add_industrial_auxiliary_features(df):
       # 温度特征
       df['temp_mean'] = ...  # 平均温度
       df['temp_extreme'] = ...  # 极端温度标记
       
       # 工业特征
       df['industrial_load'] = ...  # 工业负荷
       df['production_index'] = ...  # 生产指数滞后
       
       # 交互特征
       df['temp_x_hour'] = df['temp_mean'] * df['hour']
       
       return df
   ```

3. **训练RF基线模型**
   ```python
   from sklearn.ensemble import RandomForestRegressor
   
   rf_model = RandomForestRegressor(n_estimators=100, random_state=42)
   rf_model.fit(X_train, y_train)
   # 评估：MAE, RMSE, MAPE
   ```

4. **训练XGB进阶模型**
   ```python
   import xgboost as xgb
   
   xgb_model = xgb.XGBRegressor(
       n_estimators=1000,
       learning_rate=0.01,
       max_depth=6,
       subsample=0.8,
       colsample_bytree=0.8,
       random_state=42
   )
   xgb_model.fit(X_train, y_train, eval_set=[(X_val, y_val)], early_stopping_rounds=50)
   ```

5. **保存模型**
   ```python
   import joblib
   joblib.dump(xgb_model, 'models/xgb_baseline.pkl')
   ```

**VG核心节点✅**：**辅助特征集成**
- 交付物：辅助特征重要性对比报告（有/无辅助特征的模型性能对比）
- 可视化：辅助特征与电价关系图

#### 第9-10周（4.14-4.27）：超参数调优+特征归因 ⭐VG节点2

**执行步骤**：

1. **XGB超参数调优**
   ```python
   from sklearn.model_selection import RandomizedSearchCV
   
   param_grid = {
       'max_depth': [3, 5, 7, 9],
       'learning_rate': [0.01, 0.05, 0.1],
       'n_estimators': [500, 1000, 1500],
       'subsample': [0.6, 0.8, 1.0],
       'colsample_bytree': [0.6, 0.8, 1.0]
   }
   # 使用TimeSeriesSplit进行交叉验证
   ```

2. **⭐特征重要性分析（VG核心）**
   ```python
   # XGB内置重要性
   importance = xgb_model.feature_importances_
   features = X_train.columns
   
   # SHAP值分析（更精细的归因）
   import shap
   explainer = shap.TreeExplainer(xgb_model)
   shap_values = explainer.shap_values(X_test)
   shap.summary_plot(shap_values, X_test)
   ```
   - **重点分析**：工业特征、温度特征对电价波动的解释力
   - 保存SHAP分析图到`reports/figures/`

3. **模型对比分析**
   - RF vs XGB性能对比表
   - 残差分析（时间序列残差图）
   - 不同时间段的预测表现（高峰期vs低谷期）

4. **发送建模周报**
   - 最优模型参数
   - 特征重要性排名Top 10
   - SHAP分析关键发现

**里程碑⭐**：完成模型训练与优化，commit：`[MODEL] 完成XGB建模与特征重要性分析`

**VG核心节点✅**：**模型对比+特征归因**
- 交付物：特征重要性排名表、SHAP归因分析图、模型对比报告

---

### 🔴 阶段4：Streamlit开发（5月1日-5月20日，3周）⭐VG核心阶段

#### 第11-12周（5.1-5.11）：应用框架+核心功能

**执行步骤**：

1. **搭建应用框架（4大页面）**
   ```
   app/
   ├── app.py              # 主入口
   ├── pages/
   │   ├── 1_📊_数据概览.py
   │   ├── 2_🔮_电价预测.py
   │   ├── 3_📈_波动归因.py
   │   └── 4_📋_模型报告.py
   ```

2. **实现核心电价预测功能**
   ```python
   # pages/2_🔮_电价预测.py
   import streamlit as st
   
   st.title('瑞典电价预测')
   
   # 用户输入：选择区域、时间范围
   region = st.selectbox('选择电价区域', ['SE1', 'SE2', 'SE3', 'SE4'])
   forecast_horizon = st.slider('预测时长（小时）', 1, 72, 24)
   
   # 加载模型
   model = joblib.load('models/xgb_best.pkl')
   
   # 预测并展示
   if st.button('开始预测'):
       prediction = model.predict(input_features)
       st.line_chart(prediction)
       st.download_button('下载预测结果', prediction.to_csv())
   ```

3. **基础归因分析功能**
   - 单点预测的特征贡献展示
   - 瀑布图展示特征正负贡献

#### 第13周（5.12-5.20）：工业特征归因+UI优化 ⭐VG节点3

**执行步骤**：

1. **⭐工业/温度辅助特征归因可视化（VG核心）**
   ```python
   # pages/3_📈_波动归因.py
   st.header('电价波动归因分析')
   
   # 选择特定时间点
   selected_time = st.datetime_input('选择分析时间点')
   
   # SHAP力图（Force Plot）
   shap.force_plot(explainer.expected_value, shap_values[selected_idx], 
                   features.iloc[selected_idx], matplotlib=True)
   st.pyplot()
   
   # 工业特征专项分析
   st.subheader('工业负荷影响分析')
   industrial_features = ['industrial_load', 'production_index', 'temp_x_hour']
   for feat in industrial_features:
       st.write(f"{feat}: 贡献值 {shap_values[selected_idx][feat]:.2f}")
   ```

2. **UI美化+交互优化**
   - 使用`st.columns`布局优化
   - 添加侧边栏导航
   - 使用`plotly`替代matplotlib实现交互式图表
   - 添加加载动画`st.spinner`

3. **全功能测试**
   - 端到端测试：数据加载→预测→归因
   - 边界测试：极端值输入处理
   - 性能测试：预测响应时间<3秒

4. **编写应用使用说明**
   - 在README中添加应用启动指南
   - 添加使用截图/GIF演示

**里程碑⭐**：完成Streamlit应用，commit：`[APP] 完成Streamlit应用开发与工业特征归因可视化`

**VG核心节点✅**：**波动归因可视化**
- 交付物：可交互的工业特征归因页面、SHAP力图展示

---

### 🟣 阶段5：报告&代码重构（5月21日-5月31日，2周）

#### 第14周（5.21-5.25）：报告草稿+伦理反思

**执行步骤**：

1. **撰写`report.pdf`草稿**
   - 结构建议：
     1. 摘要（200字）
     2. 引言（背景与问题定义）
     3. 数据描述（来源、清洗过程）
     4. 方法论（特征工程、模型选择）
     5. 结果（模型性能、特征重要性）
     6. 应用展示（Streamlit功能截图）
     7. 伦理反思（150-200字）
     8. 结论与展望

2. **完成伦理反思（150-200字）**
   - 思考点：
     - 电价预测对能源市场的影响
     - 模型偏见（是否对某些区域/时段预测更准）
     - 数据隐私（工业数据脱敏处理）
     - 模型误用的风险

3. **初步代码PEP8检查**
   ```bash
   pip install flake8 black
   flake8 src/ --max-line-length=100
   black src/ --line-length=100
   ```

#### 第15周（5.26-5.31）：代码重构+报告定稿 ⭐VG节点4

**执行步骤**：

1. **⭐代码全面重构（VG核心）**
   - **PEP8规范**：每行<100字符，函数<50行
   - **文档字符串**：所有类和方法添加Google风格docstring
   ```python
   def add_power_features(df: pd.DataFrame) -> pd.DataFrame:
       """
       添加电力相关时间特征。
       
       Args:
           df: 包含'price'列和DatetimeIndex的DataFrame
           
       Returns:
           添加特征后的DataFrame
           
       Example:
           >>> df = pd.DataFrame({'price': [100, 110]}, index=pd.date_range('2024-01-01', periods=2, freq='H'))
           >>> add_power_features(df)
       """
   ```
   - **类型注解**：函数参数和返回值添加类型提示
   - **错误处理**：添加try-except和输入验证

2. **完善`main.py`一键运行流程**
   ```python
   # main.py
   def main():
       # 1. 数据加载
       # 2. 特征工程
       # 3. 模型训练
       # 4. 模型评估
       # 5. 保存结果
       pass
   
   if __name__ == '__main__':
       main()
   ```

3. **报告终版**
   - 插入所有图表（EDA、特征重要性、SHAP分析、应用截图）
   - 检查字数和格式要求
   - 导出PDF

4. **提前提交测试**
   - 开放导师GitHub仓库权限
   - 测试新环境克隆+运行
   - 确认所有文件已推送

**里程碑⭐**：完成代码重构与报告，commit：`[DOCS] 完成代码重构、文档完善与报告终版`

**VG核心节点✅**：**代码规范+报告定稿**
- 交付物：符合PEP8规范的代码库、完整项目报告

---

### 🟣 阶段6：最终提交（6月1日-6月5日，1周）

**执行步骤**：

1. **新环境全流程可复现测试**
   ```bash
   # 创建全新虚拟环境
   python -m venv test_env
   source test_env/bin/activate
   
   # 克隆仓库
   git clone https://github.com/yourusername/swedish-electricity-forecast.git
   cd swedish-electricity-forecast
   
   # 安装依赖
   pip install -r requirements.txt
   
   # 运行主流程
   python main.py
   
   # 启动应用
   cd app && streamlit run app.py
   ```
   - 记录所有报错并修复
   - 确认数据下载链接有效

2. **修复遗留问题**
   - 处理最后的warnings
   - 完善README（添加故障排除指南）

3. **⭐Omniway平台正式提交**
   - 检查提交文件清单：
     - [ ] 完整代码仓库（GitHub链接）
     - [ ] `report.pdf`
     - [ ] `README.md`
     - [ ] 模型文件（.pkl）
     - [ ]  requirements.txt
   - 确认提交格式符合要求

4. **发送最终确认邮件给导师**
   - GitHub仓库链接
   - 提交确认截图
   - 项目总结（遇到的挑战与解决方案）

**里程碑⭐**：项目正式提交，commit：`[SUBMIT] 最终提交版本`

---

## 每周五复盘清单

```markdown
## 本周复盘模板（每周五填写）

### 本周完成
- [ ] 任务1
- [ ] 任务2

### 遇到的问题
- 问题描述 + 解决方案

### 下周计划
- [ ] 优先级1任务
- [ ] 优先级2任务

### 需要帮助
- 需要导师支持的事项
```

---

## 关键成功要素

1. **Git管理**：每个⭐里程碑必须commit，使用清晰前缀（[INIT], [DATA], [FEATURE], [MODEL], [APP], [DOCS], [SUBMIT]）

2. **VG核心节点**：这4个节点是获得优良等级的关键，需单独整理成果文档：
   - ✅ 辅助特征集成（4月13日前）
   - ✅ 模型对比+特征归因（4月27日前）
   - ✅ 波动归因可视化（5月20日前）
   - ✅ 代码规范+报告定稿（5月31日前）

3. **缓冲时间**：每个阶段预留1-2天缓冲，避免最后赶工

4. **导师沟通**：每周五发送进度更新，遇到阻塞及时求助
